h1('mat-dialog-title') Enter you channelId
div('mat-dialog-content')
    mat-form-field(appearance="fill" color="accent")
        mat-label Channel ID
        input('matInput' 
            placeholder="Channel ID" 
            name="channelId"  
            '(change)'="data.channelId = $event.target.value")
        span('matSuffix')
            a('mat-icon-button' href="https://support.google.com/youtube/answer/3250431?hl=en" target="_blank")
                mat-icon(aria-label="How to get your channel ID") help
div('mat-dialog-actions')
    button('mat-button' '(click)'="onNoClick()") No Thanks
    button('mat-button' '[mat-dialog-close]'="data.channelId" 'cdkFocusInitial') Ok